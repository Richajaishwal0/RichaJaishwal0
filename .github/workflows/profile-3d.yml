name: GitHub-Profile-3D-Contrib

on:
  schedule: # 03:00 JST == 18:00 UTC
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Richajaishwal0
    steps:
      - uses: actions/checkout@v3
      - uses: Richajaishwal0/RichaJaishwal0@main
        env:
          GITHUB_TOKEN: ${{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}}
          USERNAME: ${{ Richajaishwal0}}
      - name: Commit & Push
        run: |
          git config user.name Richajaishwal0
          git config user.email richajaiswalhome@gmail.com
          git add -A .
          git commit -m "generated"
          git push
